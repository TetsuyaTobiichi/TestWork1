using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bills : MonoBehaviour,IStuff
{
    // Start is called before the first frame update
    public float GetBonus(){
        return 0.1f;
    }
}
